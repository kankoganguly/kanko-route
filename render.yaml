services:
  - type: web
    name: kanko-route
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python route_server.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
